import React from 'react';
import preloader from '../../../assets/img/loading.gif';

const Preloader = () => {
    return (
        <>
            <img src={preloader} />
        </>
    );
};

export default Preloader;