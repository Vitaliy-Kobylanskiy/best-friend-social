import React from 'react';
import cl from './Footer.module.scss';

const Footer = () => {
    return (
        <footer className={cl.footer}>
            <p className={cl.text}>Best Friends 2015</p>
        </footer>
    );
};

export default Footer;